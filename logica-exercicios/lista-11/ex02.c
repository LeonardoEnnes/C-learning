#include <stdio.h>
/*Leia dois valores inteiros X e Y. Imprima a soma de todos os valores ímpares entre eles.

Entrada
O arquivo de entrada contém dois valores inteiros.

Saída
O programa deve imprimir um número inteiro.
 Esse número é a soma de todos os valores ímpares entre os dois valores de entrada 
 que devem caber em um número inteiro. */
int main(){
  int x, y;
  int somaImpares = 0;

  printf("Digite o valor de X: ");
  scanf("%d", &x);
  printf("Digite o valor de Y: ");
  scanf("%d", &y);

  
  
  return 0;
}