#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>
#include <math.h>

int main(){
 
  double A = sqrt(9); // raiz quadrada
  double B = pow(2, 4); // A pow()função retorna o valor de x à potência de y ( x y ):
  int c = round(3.33); // Retorna o inteiro mais próximo do número passado (não se esquecer que é numero inteiro)
  int D = ceil(3.55); // arredonda para cima
  int E = floor(3.88); //arredonda para baixo
  double F = fabs(-100);
  double G = log(3); // logaritimo
  double H = sin(45); // seno
  double I = cos(45); // cosseno
  double J = tan(45); // tangente

  printf("\n %lf", A);




  return 0;
}